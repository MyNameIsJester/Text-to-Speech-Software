<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="TravelTracker.ScannerPage"
             Title="Quét mã QR Trạm Xe Buýt"
             BackgroundColor="Black">

    <Grid>
        <zxing:CameraBarcodeReaderView x:Name="cameraReader" 
                                       BarcodesDetected="CameraReader_BarcodesDetected" />

        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="20">
            <Border Stroke="Orange" StrokeThickness="4" StrokeShape="RoundRectangle 10" 
                    WidthRequest="250" HeightRequest="250" BackgroundColor="Transparent" />
            <Label Text="Đưa mã QR tại trạm vào trong khung" 
                   TextColor="White" FontAttributes="Bold" FontSize="16" 
                   HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Grid>

</ContentPage>